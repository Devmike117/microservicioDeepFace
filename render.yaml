services:
  - type: web
    name: deepface-service
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn deepface_service:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
